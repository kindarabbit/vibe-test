# Technical Design

## 1. 문서 목적

이 문서는 서비스의 기술 구현 방향을 정리한다.  
제품의 가치나 사용자 문제는 Product Brief에서 다루고, 이 문서에서는 실제 개발자가 구현할 구조를 정의한다.

---

## 2. Architecture Overview

## 전체 구조

```text
User
→ Next.js App
→ Pages / Routes
→ UI Components
→ Feature Logic
→ Client State
→ Storage
→ Future Backend
```

## 이번 MVP의 구현 범위

* 단일 사용자 기준
* 프론트엔드 중심 구현
* 서버 DB 없이 localStorage 또는 mock data 사용
* 발표 대본 생성은 mock generator로 우선 구현
* 인증, 결제, 실시간 기능 제외
* 대용량 PPT 파일 업로드 제외
* 외부 AI API 연동은 향후 확장으로 분리

---

## 3. Tech Stack

| Area | Technology | Reason |
| --------------- | ---------------- | ------------------- |
| Framework | Next.js | App Router 기반 웹앱 구현 |
| UI Library | React | 컴포넌트 기반 UI 구성 |
| Language | TypeScript | 타입 기반 안정성 확보 |
| Styling | Tailwind CSS | 빠른 UI 스타일링 |
| AI Coding | Claude Code | 코드 생성, 수정, 검토 |
| Version Control | GitHub | 커밋, 브랜치, 비교 실험 |
| Test | Playwright later | 4회차 테스트 자동화 예정 |

---

## 4. Route Design

| Route | File Path | Purpose | Notes |
| ------ | ---------------------- | ------------- | ----------- |
| `/` | `src/app/page.tsx` | Landing Page | 서비스 소개, CTA |
| `/app` | `src/app/app/page.tsx` | Main App Page | 실제 발표 대본 생성 화면 |

---

## 5. Source Structure

```text
src/
  app/
    page.tsx
    app/
      page.tsx

  components/
    ui/
      Button.tsx
      Textarea.tsx
      Select.tsx
      Badge.tsx
      Card.tsx

    layout/
      AppHeader.tsx
      AppShell.tsx

  features/
    scripts/
      types.ts
      mock-data.ts
      storage.ts
      generator.ts
      components/
        ScriptForm.tsx
        ScriptPreview.tsx
        ScriptList.tsx
        ScriptCard.tsx
        ToneSelector.tsx
        DurationSelector.tsx
        FilterTabs.tsx
        EmptyState.tsx
```

## 폴더 역할

| Folder | Role |
| ------------------------------- | ---------------------- |
| `src/app` | Next.js route와 page 관리 |
| `src/components/ui` | 재사용 가능한 기본 UI 컴포넌트 |
| `src/components/layout` | 레이아웃 관련 컴포넌트 |
| `src/features/scripts` | 발표 대본 생성 기능 단위 코드 |
| `src/features/scripts/components` | scripts 기능 전용 컴포넌트 |

---

## 6. Feature Module Design

## 핵심 Feature

| Feature | Description | Priority |
| ------------------ | ------------- | -------- |
| Script Create | 사용자가 발표 내용과 옵션으로 새 대본을 생성 | Must |
| Script Preview | 생성된 대본 본문을 화면에 표시 | Must |
| Script List | 생성된 대본 목록 표시 | Must |
| Script Status Update | 대본 상태 변경 | Should |
| Script Filter | 말투, 발표 시간, 상태, 검색어 기준 필터링 | Should |
| Script Copy | 생성된 대본 본문 복사 | Should |
| Script Delete/Edit | 대본 삭제 또는 수정 | Nice |

## 이번 회차에서 구현할 Feature

* Route 구조
* Landing Page 초안
* App Page shell
* 기본 타입
* 컴포넌트 placeholder
* mock data 또는 빈 상태
* mock generator 설계

## 다음 회차로 넘길 Feature

* 실제 대본 생성 폼 동작
* 목록 상태 관리
* 상태 변경 로직
* 필터 로직
* 복사 기능
* 삭제 또는 수정 로직

---

## 7. Data Model

## 기본 타입

```ts
export type ScriptTone = "natural" | "formal" | "friendly";

export type ScriptDuration = 5 | 10 | 15;

export type ScriptStatus = "draft" | "review" | "done";

export type PresentationScript = {
  id: string;
  title: string;
  sourceText: string;
  tone: ScriptTone;
  duration: ScriptDuration;
  status: ScriptStatus;
  content: string;
  createdAt: string;
  updatedAt: string;
};
```

## 서비스별 확장 필드

| Field | Type | Required | Description |
| ------------- | ------------ | -------- | ----------- |
| `id` | `string` | Yes | 고유 ID |
| `title` | `string` | Yes | 발표 주제 또는 대본 제목 |
| `sourceText` | `string` | Yes | PPT 내용 또는 발표 키워드 |
| `tone` | `ScriptTone` | Yes | 선택한 발표 말투 |
| `duration` | `ScriptDuration` | Yes | 선택한 발표 시간 |
| `status` | `ScriptStatus` | Yes | 대본 준비 상태 |
| `content` | `string` | Yes | 생성된 발표 대본 본문 |
| `createdAt` | `string` | Yes | 생성 시각 |
| `updatedAt` | `string` | Yes | 수정 시각 |

## 서비스별 추가 필드

| Field | Type | Required | Description |
| ----- | ---- | -------- | ----------- |
| `toneLabel` | `string` | No | UI에 표시할 말투 이름 |
| `estimatedWordCount` | `number` | No | 발표 시간 기반 예상 분량 |
| `copiedAt` | `string` | No | 마지막 복사 시각 |

---

## 8. State Design

| State | Type | Purpose |
| ----------------- | --------------------- | ----------- |
| `scripts` | `PresentationScript[]` | 현재 대본 목록 |
| `formInput` | `{ title: string; sourceText: string }` | 입력 폼 상태 |
| `selectedTone` | `ScriptTone` | 말투 선택 상태 |
| `selectedDuration` | `ScriptDuration` | 발표 시간 선택 상태 |
| `selectedStatusFilter` | `ScriptStatus \| "all"` | 상태 필터 |
| `selectedToneFilter` | `ScriptTone \| "all"` | 말투 필터 |
| `selectedDurationFilter` | `ScriptDuration \| "all"` | 발표 시간 필터 |
| `searchQuery` | `string` | 검색어 상태 |
| `activeScriptId` | `string \| null` | 현재 미리보기 대상 |
| `validationError` | `string \| null` | 입력 오류 메시지 |
| `isLoading` | `boolean` | 향후 서버 연동 대비 |

## 상태 관리 방식

이번 MVP에서는 별도 상태 관리 라이브러리를 사용하지 않는다.

* React `useState`
* 필요 시 `useMemo`
* localStorage helper 함수

---

## 9. Storage Strategy

## 1차 MVP

| Option | Decision |
| ------------ | ----------- |
| DB | 사용하지 않음 |
| API Server | 사용하지 않음 |
| localStorage | 기본 저장 방식 후보 |
| mock data | 초기 화면 구성용 |

## 저장 흐름

```text
User Action
→ React State Update
→ localStorage Save
→ UI Re-render
```

## localStorage Key

```text
presentation-scripts
```

## 향후 확장 가능성

* DB 저장
* 사용자 인증
* Server Actions
* API Route
* 외부 AI API 연동

---

## 10. API Design

이번 MVP에서는 서버 API를 구현하지 않는다.

## 향후 확장 시 API 후보

| API | Method | Purpose |
| ---------------- | -------- | -------------- |
| `/api/scripts` | `GET` | 대본 목록 조회 |
| `/api/scripts` | `POST` | 대본 생성 |
| `/api/scripts/:id` | `PATCH` | 대본 수정 또는 상태 변경 |
| `/api/scripts/:id` | `DELETE` | 대본 삭제 |
| `/api/generate-script` | `POST` | AI 기반 대본 생성 |

## 이번 회차 결정

* API 구현 없음
* 서버 DB 없음
* 클라이언트 상태와 localStorage 중심
* 대본 생성은 mock generator 중심

---

## 11. Validation Rules

| Rule | Description |
| ----------------- | ---------------- |
| Required Title | 발표 제목은 비어 있을 수 없다 |
| Required Source Text | PPT 내용 또는 발표 키워드는 비어 있을 수 없다 |
| Title Length | 제목은 너무 길지 않아야 한다 |
| Source Text Length | 입력 내용은 MVP 화면에서 처리 가능한 길이여야 한다 |
| Valid Tone | 말투는 정의된 값만 허용한다 |
| Valid Duration | 발표 시간은 정의된 값만 허용한다 |
| Valid Status | 상태는 정의된 값만 허용한다 |
| No Sensitive Data | 민감 정보는 저장하지 않는다 |

---

## 12. Error Handling

| Situation | Handling |
| ------------------ | --------------- |
| 제목 미입력 | 입력 오류 메시지 표시 |
| 발표 내용 미입력 | 입력 오류 메시지 표시 |
| localStorage 읽기 실패 | 빈 배열로 fallback |
| localStorage 저장 실패 | 화면 상태는 유지하고 저장 실패 안내 표시 |
| 알 수 없는 말투값 | 기본 말투 `natural`로 fallback |
| 알 수 없는 발표 시간 | 기본 시간 `5`로 fallback |
| 알 수 없는 상태값 | 기본 상태 `draft`로 fallback |
| 데이터 없음 | EmptyState 표시 |
| 필터 결과 없음 | 검색 결과 없음 EmptyState 표시 |

---

## 13. Accessibility Considerations

* 입력 필드는 label을 가진다.
* 버튼 텍스트는 기능을 설명한다.
* 말투와 발표 시간 선택값은 스크린 리더가 인지할 수 있어야 한다.
* 색상만으로 상태를 구분하지 않는다.
* 주요 영역은 heading 구조를 가진다.
* 키보드로 주요 액션을 수행할 수 있어야 한다.
* 생성 결과 영역은 변경 내용을 인지할 수 있도록 적절한 live region을 고려한다.
* 모바일 화면에서 생성, 복사, 필터 액션이 가려지지 않아야 한다.

---

## 14. Security Considerations

이번 MVP에서 지킬 보안 원칙

* API key를 코드에 넣지 않는다.
* `.env` 파일을 GitHub에 올리지 않는다.
* 민감한 개인정보를 저장하지 않는다.
* localStorage에는 민감 정보 저장을 피한다.
* 인증이 필요한 기능은 이번 MVP에서 제외한다.
* 외부 AI API를 붙일 경우 API key는 서버 환경 변수로만 관리한다.

---

## 15. Decision Log

| Decision | Reason | Consequence |
| --------------------- | ----------------------------- | ---------------- |
| Next.js App Router 사용 | 현재 Next.js 기본 구조와 수업 방향에 적합 | `src/app` 기준 라우팅 |
| TypeScript 사용 | 데이터 구조와 컴포넌트 props를 명확히 하기 위해 | 초기 작성량 증가 |
| localStorage 우선 | 4회차 안에 MVP 완성하기 위해 | 다중 사용자 기능 제외 |
| 단일 Entity 중심 | 범위 통제 | 복잡한 관계형 데이터 제외 |
| API 미구현 | 시간 제한과 학습 목표 고려 | 서버 기능은 향후 확장 |
| mock generator 사용 | 외부 AI API 없이 핵심 흐름 검증 가능 | 실제 AI 품질 검증은 다음 단계로 이동 |
| scripts feature module 사용 | 서비스의 핵심 대상이 발표 대본이기 때문 | item 기반 템플릿보다 도메인 의미가 명확함 |

---

## 16. Implementation Notes

3회차에서 구현할 때 Claude Code는 다음 순서를 따른다.

1. 현재 파일 구조 확인
2. `01. Product Brief.md`, `02. Requirements Spec.md`, `03. UX UI Spec.md`, `04. Technical Design.md` 읽기
3. 수정 전 계획 제안
4. 작은 단위로 구현
5. 실행 또는 build로 검증
6. 변경 파일 요약
7. commit message 제안

---

## 17. Open Questions

| Question | Decision Needed By |
| --------------------- | ------------------ |
| 서비스에서 대본 목록을 몇 개까지 저장할 것인가? | 3회차 시작 전 |
| 말투 옵션은 `자연스럽게`, `공식적으로`, `친근하게` 3개로 충분한가? | 3회차 시작 전 |
| 발표 시간은 5분, 10분, 15분만 제공할 것인가? | 3회차 시작 전 |
| 상태값은 작성 중, 검토 중, 완료 3단계로 충분한가? | 3회차 시작 전 |
| 삭제 기능을 MVP에 넣을 것인가? | 3회차 중 |
| 필터는 상태, 말투, 발표 시간을 모두 포함할 것인가? | 3회차 시작 전 |
