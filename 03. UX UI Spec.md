# UX / UI Spec

## 1. Design Reference

Follow:

- docs/DESIGN.md
- DESIGN.md

## 2. Screen Map

| Screen | Route | Purpose |
|---|---|---|
| Landing Page | `/` | 서비스 소개와 앱 진입 |
| App Page | `/app` | 발표 대본 생성, 목록 확인, 상태 변경, 필터 사용 |

## 3. Landing Page

### Purpose

서비스의 문제, 가치, 핵심 기능을 설명하고 사용자를 앱 화면으로 이동시킨다.

### Required Sections

- Hero
- Problem
- Core Features
- CTA Button

### Key Copy

- Headline: 발표 자료를 대본으로 바꾸는 가장 빠른 방법
- Subheadline: PPT 내용이나 발표 키워드를 입력하면 원하는 말투와 발표 시간에 맞춰 자연스러운 발표 대본을 만들어준다.
- CTA: 발표 대본 만들기

## 4. App Page

### Purpose

사용자가 실제 기능을 수행하는 화면이다.

### Required Areas

- Header
- Input Form
- Tone Option Selector
- Duration Selector
- Generated Script Preview
- Filter Area
- List Area
- Empty State
- Status Action
- Copy Action

## 5. Component Plan

| Component | Purpose | Requirement Link |
|---|---|---|
| AppHeader | 화면 제목과 간단한 사용 맥락 제공 | FR-004 |
| ScriptForm | 발표 주제, PPT 내용 또는 발표 키워드 입력 | FR-001 |
| ToneSelector | 자연스럽게, 공식적으로, 친근하게 등 말투 선택 | FR-002 |
| DurationSelector | 5분, 10분, 15분 등 발표 시간 선택 | FR-003 |
| ScriptPreview | 생성된 발표 대본 본문 표시 | FR-004 |
| ScriptToolbar | 대본 복사, 상태 변경 등 주요 액션 제공 | FR-006, FR-008 |
| FilterTabs | 상태, 말투, 발표 시간 기준 필터 | FR-007 |
| SearchInput | 생성된 대본을 키워드로 검색 | FR-007 |
| ScriptList | 생성된 발표 대본 목록 표시 | FR-005 |
| ScriptCard | 대본 하나의 제목, 말투, 시간, 상태 표시 | FR-005, FR-006 |
| EmptyState | 생성된 대본 또는 검색 결과가 없을 때 안내 | FR-005, FR-007 |

## 6. Interaction Rules

- 대본 생성 후 발표 주제와 내용 입력창은 초기화된다.
- 말투와 발표 시간 선택값은 다음 생성에도 재사용할 수 있도록 유지한다.
- 필수값이 없으면 생성 버튼을 눌러도 대본이 추가되지 않고 안내 문구를 표시한다.
- 생성된 대본은 결과 영역에 즉시 표시되고 목록의 맨 위에 추가된다.
- 상태 변경 버튼을 누르면 작성 중, 검토 중, 완료 순서로 즉시 변경된다.
- 필터나 검색어를 변경하면 목록이 즉시 바뀐다.
- 복사 버튼을 누르면 대본 본문이 클립보드에 복사되고 복사 완료 상태를 표시한다.
- 검색 또는 필터 결과가 없으면 Empty State를 표시한다.

## 7. Accessibility Rules

- 모든 입력 필드에는 label이 있어야 한다.
- 말투와 발표 시간 선택 컨트롤은 현재 선택 상태를 텍스트와 시각 상태로 함께 제공해야 한다.
- 버튼 텍스트는 기능을 설명해야 한다.
- 아이콘 버튼을 사용할 경우 접근 가능한 이름을 제공해야 한다.
- 색상만으로 상태를 구분하지 않는다.
- 주요 영역은 heading 구조를 가진다.
- 생성 결과 영역은 스크린 리더가 변경 내용을 인지할 수 있도록 적절한 라이브 영역을 고려한다.
- 모바일 화면에서도 입력, 생성, 복사, 필터 액션이 가려지지 않아야 한다.
