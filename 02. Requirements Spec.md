# Requirements Spec

## 1. Actors

| Actor | Description |
|---|---|
| Primary User | 발표 과제나 팀 프로젝트를 준비하며 PPT 내용 또는 발표 키워드를 발표 대본으로 정리하려는 대학생 |
| Optional Admin | 이번 MVP에서는 제외하며, 모든 기능은 단일 사용자 기준으로 처리 |

## 2. Main Use Cases

### UC-001. 발표 대본 생성

- Actor: Primary User
- Goal: PPT 내용 또는 발표 키워드를 입력하고, 말투와 발표 시간을 선택해 발표 대본을 생성한다.
- Precondition: 사용자가 앱 화면에 접속해 입력 폼을 볼 수 있다.
- Main Flow:
  1. 사용자는 발표 주제, PPT 내용 또는 발표 키워드를 입력한다.
  2. 사용자는 말투 옵션과 발표 시간을 선택한다.
  3. 사용자는 생성 버튼을 눌러 발표 대본을 생성한다.
- Alternative Flow: 필수 입력값이 비어 있으면 사용자에게 입력이 필요하다는 안내를 표시한다.
- Result: 선택한 말투와 발표 시간에 맞는 발표 대본이 결과 화면에 표시된다.

### UC-002. 생성 결과 확인

- Actor: Primary User
- Goal: 생성된 발표 대본을 화면에서 확인한다.
- Precondition: 사용자가 발표 대본을 1개 이상 생성했다.
- Main Flow:
  1. 사용자는 대본 생성 결과 영역을 확인한다.
  2. 사용자는 생성된 대본의 제목, 선택한 말투, 발표 시간, 본문을 확인한다.
  3. 사용자는 필요한 경우 이전에 생성한 대본 목록을 확인한다.
- Alternative Flow: 생성된 대본이 없으면 빈 상태 메시지를 표시한다.
- Result: 사용자는 생성된 발표 대본을 읽고 발표 준비에 활용할 수 있다.

### UC-003. 대본 상태 변경

- Actor: Primary User
- Goal: 생성된 대본의 준비 상태를 변경한다.
- Precondition: 생성된 발표 대본이 목록에 존재한다.
- Main Flow:
  1. 사용자는 대본 목록에서 특정 대본을 선택한다.
  2. 사용자는 상태 변경 버튼을 누른다.
  3. 시스템은 해당 대본의 상태를 작성 중, 검토 중, 완료 중 하나로 변경한다.
- Alternative Flow: 상태 변경 대상이 없으면 상태 변경 버튼을 비활성화한다.
- Result: 사용자는 대본별 준비 진행 상황을 구분할 수 있다.

### UC-004. 필터 또는 검색

- Actor: Primary User
- Goal: 생성된 대본을 말투, 발표 시간, 상태 또는 검색어 기준으로 빠르게 찾는다.
- Precondition: 생성된 발표 대본이 1개 이상 존재한다.
- Main Flow:
  1. 사용자는 필터 조건 또는 검색어를 입력한다.
  2. 시스템은 조건에 맞는 대본만 목록에 표시한다.
  3. 사용자는 원하는 대본을 선택해 내용을 확인한다.
- Alternative Flow: 조건에 맞는 대본이 없으면 검색 결과가 없다는 안내를 표시한다.
- Result: 사용자는 필요한 발표 대본을 빠르게 찾을 수 있다.

## 3. Functional Requirements

| ID | Requirement | Priority |
|---|---|---|
| FR-001 | 사용자는 PPT 내용 또는 발표 키워드를 입력해 발표 대본을 생성할 수 있다. | Must |
| FR-002 | 사용자는 말투 옵션을 선택할 수 있다. | Must |
| FR-003 | 사용자는 발표 시간을 선택할 수 있다. | Must |
| FR-004 | 사용자는 생성된 발표 대본을 화면에서 확인할 수 있다. | Must |
| FR-005 | 사용자는 생성된 발표 대본을 목록에서 확인할 수 있다. | Must |
| FR-006 | 사용자는 발표 대본의 상태를 변경할 수 있다. | Should |
| FR-007 | 사용자는 말투, 발표 시간, 상태 또는 검색어로 대본을 필터링할 수 있다. | Should |
| FR-008 | 사용자는 생성된 발표 대본을 복사할 수 있다. | Should |
| FR-009 | 사용자는 생성된 발표 대본을 삭제하거나 수정할 수 있다. | Nice |

## 4. Non-functional Requirements

| ID | Requirement |
|---|---|
| NFR-001 | 모바일 화면에서도 핵심 기능을 사용할 수 있어야 한다. |
| NFR-002 | 버튼과 입력 필드는 접근 가능한 이름을 가져야 한다. |
| NFR-003 | 민감한 정보는 GitHub 저장소에 커밋하지 않는다. |
| NFR-004 | 기본 MVP는 단일 사용자 기준으로 구현한다. |
| NFR-005 | 사용자는 앱 진입 후 1분 안에 첫 발표 대본을 생성할 수 있어야 한다. |
| NFR-006 | 생성 결과는 사용자가 발표 흐름을 쉽게 파악할 수 있도록 문단 단위로 구분되어야 한다. |

## 5. Acceptance Criteria

### AC-001. 발표 대본 생성

Given 사용자가 발표 내용 또는 키워드, 말투, 발표 시간을 입력했을 때  
When 생성 버튼을 누르면  
Then 선택한 조건에 맞는 발표 대본이 결과 화면에 표시된다.

### AC-002. 생성 결과 확인

Given 발표 대본이 하나 이상 생성되어 있을 때  
When 사용자가 앱 화면을 열거나 생성 결과 영역을 확인하면  
Then 생성된 대본의 본문, 말투, 발표 시간이 표시된다.

### AC-003. 상태 변경

Given 목록에 생성된 발표 대본이 있을 때  
When 사용자가 상태 변경 버튼을 누르면  
Then 해당 대본의 상태가 변경되어 목록에 반영된다.

### AC-004. 필터

Given 여러 말투, 발표 시간 또는 상태의 대본이 있을 때  
When 사용자가 특정 필터나 검색어를 선택하면  
Then 조건에 맞는 대본만 표시된다.

### AC-005. 대본 복사

Given 생성된 발표 대본이 결과 화면에 표시되어 있을 때  
When 사용자가 복사 버튼을 누르면  
Then 대본 본문이 클립보드에 복사되고 복사 완료 안내가 표시된다.

## 6. Requirement Traceability Lite

| Requirement ID | Use Case | Acceptance Criteria | Test Candidate |
|---|---|---|---|
| FR-001 | UC-001 | AC-001 | E2E create script |
| FR-002 | UC-001 | AC-001 | E2E select tone |
| FR-003 | UC-001 | AC-001 | E2E select duration |
| FR-004 | UC-002 | AC-002 | E2E view generated script |
| FR-005 | UC-002 | AC-002 | E2E view script list |
| FR-006 | UC-003 | AC-003 | E2E update script status |
| FR-007 | UC-004 | AC-004 | E2E filter scripts |
| FR-008 | UC-002 | AC-005 | E2E copy script |
